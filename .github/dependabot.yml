version: 2
updates:
  - directory: /
    labels:
      - dependencies
    package-ecosystem: npm
    schedule:
      interval: monthly
registries:
  npm-github:
    token: ${{ secrets.NPM_TOKEN }}
    type: npm-registry
    url: https://registry.npmjs.org
